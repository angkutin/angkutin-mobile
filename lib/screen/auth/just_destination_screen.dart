import 'package:flutter/material.dart';

class JustDestination extends StatelessWidget {
  const JustDestination({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("Hello :)"),
        ),
      ),
    );
  }
}